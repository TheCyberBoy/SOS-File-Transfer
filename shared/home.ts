// The home page has no other logic — this exists solely to wire the header
// theme toggle, the one interactive element index.html has.

import { wireThemeToggle } from "./theme";

wireThemeToggle();
